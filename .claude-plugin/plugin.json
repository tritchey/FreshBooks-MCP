{
  "name": "freshbooks",
  "description": "MCP server and skills for logging and reconciling time entries in FreshBooks, including the hours-report skill it computes hours with.",
  "version": "0.1.0",
  "author": {
    "name": "Tim Ritchey"
  },
  "mcpServers": {
    "freshbooks": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}", "freshbooks-mcp"]
    }
  }
}
